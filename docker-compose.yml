version: '3'

services:
  app:
    image: node:18 
    working_dir: /app
    volumes:
      - .:/app 
      - /app/node_modules
      - ./database:/app/database  
    ports:
      - "3030:3030"
    command: npm start
    environment:
      - NODE_ENV=development
